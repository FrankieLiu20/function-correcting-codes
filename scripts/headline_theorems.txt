# Headline results of the FCC development.
#
# Every name in this manifest must have a `#print axioms` declaration in
# FCC/AxiomCheck.lean, and every `#print axioms` there must be listed here (both
# directions are enforced by scripts/axioms_check.ps1).  Adding or removing a
# headline theorem therefore requires an explicit change here.
#
# Phase 0 (paper-independent base layer):
mem_ball_self
wt_le_wt_add_hammingDist
wt_zero
#
# Phase 1a (counting API):
ball_eq_biUnion_sphere
ball_eq_univ_of_le
card_ball_univ
card_word
disjoint_sphere
hammingDist_eq_card_diffSet
#
# Phase 1a (paper-example regression tests, FCC/Examples.lean):
ex6_cdrm_matches
ex6_length_three
ex6_no_length_two
ex7_drm_matches
#
# Phase 3.2 (the paper's own results, in paper order):
# §II (the (f,t)-FCC bounds quoted from [1]):
optimalRedundancy_eq_fdm
optimalRedundancy_ge_drm
optimalRedundancy_le_fdm
two_mul_le_optimalRedundancy
# §IV (with data protection):
N_cdrm_le_Nconst
N_drmData_le_N_cdrm_add
optimalRedundancyData_eq_N_drmData
optimalRedundancyData_ge_N_subset
optimalRedundancyData_ge_Nconst_sub
optimalRedundancyData_le_N_cdrm_add
two_mul_le_optimalRedundancyData
